{
  "name": "n-dim-particle-life",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.43.1",
    "@types/node": "^25.9.1",
    "@webgpu/types": "^0.1.70",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "wrangler": "^4.107.1"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}
